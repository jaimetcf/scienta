[project]
name = "reflex-ui"
version = "0.1.0"
description = "UI for the scienta application"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openai>=1.0.0",
    "reflex>=0.9.0",
    "psycopg[binary,pool]>=3.2.0",
    "bcrypt>=4.0",
]
